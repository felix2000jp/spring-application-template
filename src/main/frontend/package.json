{
  "name": "frontend",
  "version": "1.0.0",
  "description": "",
  "author": "Joao Felix",
  "license": "ISC",
  "private": true,
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "concurrently 'npm run dev:js' 'npm run dev:css'",
    "build": "concurrently 'npm run build:js' 'npm run build:css'",
    "clean": "rimraf node_modules ../resources/static/dist",
    "dev:js": "esbuild index.js --bundle --outfile=../resources/static/dist/main.js --watch",
    "build:js": "esbuild index.js --bundle --outfile=../resources/static/dist/main.js --minify",
    "dev:css": "npx @tailwindcss/cli -i styles.css -o ../resources/static/dist/main.css --watch",
    "build:css": "npx @tailwindcss/cli -i styles.css -o ../resources/static/dist/main.css --minify"
  },
  "dependencies": {
    "@tailwindcss/cli": "^4.0.5",
    "alpinejs": "^3.14.8",
    "htmx.org": "^2.0.4",
    "tailwindcss": "^4.0.5"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "esbuild": "^0.25.0",
    "rimraf": "^6.0.1"
  }
}
